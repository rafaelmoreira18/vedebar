# Sanity CMS Configuration
# Get these values from https://www.sanity.io/manage

# Project ID (found in your Sanity project settings)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here

# Dataset name (usually 'production' or 'development')
NEXT_PUBLIC_SANITY_DATASET=production

# Revalidation Secret (generate a random string)
# Used for on-demand ISR revalidation webhook from Sanity
# Example: openssl rand -base64 32
REVALIDATION_SECRET=your_random_secret_here_replace_me

# Existing RD Station Configuration
RD_STATION_TOKEN=your_rd_station_token_here
